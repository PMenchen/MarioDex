/**
 * Environment configuration for production
 * 
 * apiUrl: URL base de la API Laravel en produccion
 * Modificar esta URL cuando se despliegue el backend
 */
export const environment = {
  production: true,
  apiUrl: 'https://mini-pokedex-api.onrender.com/api'
};
